spring:
  datasource:
    url: jdbc:h2:mem:auditdb-test;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;MODE=PostgreSQL
  jpa:
    show-sql: false

logging:
  level:
    com.crok4it: INFO
    org.springframework.transaction.interceptor: OFF
